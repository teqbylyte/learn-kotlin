// Singleton: This is used to have only one instance of a class.

/**
 * A singleton created using the ```object``` keyword
 */
object Config {
    // This will always return the same instance
    init {
        println("Config setup")
    }
}